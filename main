
#### `pyproject.toml`
```toml
[project]
name = "vigilant-waffle"
version = "0.1.0"
description = "A vigilant monitoring tool with a waffle twist"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Your Name"}]

dependencies = [
    "rich>=13.7.0",
    "typer>=0.12.0",
    "pyyaml>=6.0",
    "watchdog>=4.0.0",
]

[project.scripts]
vigilant-waffle = "vigilant_waffle.main:app"

[tool.ruff]
line-length = 100
